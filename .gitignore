# Python
__pycache__/
*.py[cod]
*.egg-info/

# Compiled solvers
solvers/lbm/lbm2d
solvers/**/*.o

# Simulation scratch (regenerated by demos) — keep the final gallery media
results/_work*/
results/_still_check.png
*.bin

# OS / editor
.DS_Store
.idea/
.vscode/

# Keep the gallery outputs (negated so the rules above don't catch them)
!results/*.gif
!results/*.mp4

# stills (scratch previews)
results/_still*.png
solvers/incompressible/ins2d
solvers/compressible/euler2d
solvers/sph/sph2d
results/_mask_check.png

# PyInstaller / Windows build artifacts
build/
dist/
*.spec
solvers/**/*.exe
bin/
.venv/
